doctype html
html
  head
    title Detalle de Admisión
  body
    h1 Detalle de Admisión ##{admision.id}

    p
      if admision.Paciente
        | Paciente: #{admision.Paciente.nombre} #{admision.Paciente.apellido}
      else
        | Paciente no asignado

    p
      if admision.Turno
        | Turno: #{admision.Turno.fecha} a las #{admision.Turno.hora}
      else
        | Turno no asignado

    p
      | Motivo: #{admision.motivo}

    
    a(href='/admision/vista') ← Volver a la lista
    a(href=`/admision/vista/${admision.id}/editar`) 📝 Editar esta admisión
