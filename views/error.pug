extends layout.pug

block content
  .text-center(class=['py-10'])
    h2(class=['text-danger', 'text-4xl', 'font-extrabold', 'mb-4', 'pb-4', 'border-b-2', 'border-danger', 'inline-block']) ⚠️ Error: #{title}
    p(class=['text-lg', 'text-gray-700', 'mb-8']) #{message}
    p(class=['text-md', 'text-gray-600'])
      | Puedes intentar regresar a la
      a(href="/", class=['text-primary', 'hover:underline', 'ml-1'])
        | página de inicio
      |.